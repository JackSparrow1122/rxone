Place Flyway SQL migrations in this directory.

The `users.extra_fields` drop migration is intentionally not added yet.
Roll out the application code change first, then add a versioned SQL file here.
